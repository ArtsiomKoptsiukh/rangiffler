rootProject.name = 'rangiffler'

def modules = [
        'rangiffler-api',
        'rangiffler-auth',
        'rangiffler-e2e',
]

modules.each { m ->
    if (file(m).exists()) {
        include m
    }
}