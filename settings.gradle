rootProject.name = 'rangiffler'

def modules = [
        'rangiffler-gateway',
        'rangiffler-auth',
        'rangiffler-e2e',
]

modules.each { m ->
    if (file(m).exists()) {
        include m
    }
}